var arry = [];

function Book(name, author, readOrNot){
    this.name = name;
    this.author = author;
    this.readOrNot = readOrNot;
}


var button = document.getElementsByTagName("button")[0];
var left = document.getElementsByClassName("left")[0];
var bookName; 
var author;
var readBook;

// var DIV = document.createElement("div");
// document.body.appendChild(DIV);

left.appendChild(containerDIV);

    button.addEventListener("click", ()=>{
        bookName = document.getElementById("name").value;
         author = document.getElementById("author").value;
         readBook = document.getElementById("read").value;
         var newBook = createBook(bookName, author, readBook);
        arry.push(newBook);
         console.log(newBook); 

         DIV.innerHTML = '';

         arry.forEach(function(element) {
            var header = document.createElement("h1");
            var subHeader = document.createElement("h3");
            var paragraph = document.createElement("p");
            var containerDIV = document.createElement("div");
            containerDIV.classList.add("bookDIV");
            header.innerText = element.name;
            subHeader.innerText = element.author;
            paragraph.innerText = element.readOrNot;
            left.appendChild(containerDIV);
            containerDIV.appendChild(header);
            containerDIV.appendChild(subHeader);
            containerDIV.appendChild(paragraph);
        });
        });

     function createBook(bookName, author, readBook){
        return new Book(bookName, author, readBook);
    }

    //  arry.forEach(function(element) {
    //     var DIV = document.createElement("div");
    //     var header = document.createElement("h1");
    //     header.innerText = element.name;
    //     // Assuming you want to append each created h1 element to the body
    //     document.body.appendChild(DIV);
    //     DIV.appendChild(header);
    // });